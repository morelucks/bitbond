{
  "name": "@bitbond/contract",
  "version": "1.0.0",
  "description": "BitBond smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "deploy": "hardhat deploy --network regtest",
    "deploy:mainnet": "hardhat deploy --network mainnet",
    "verify": "hardhat verify --network regtest",
    "test": "hardhat test",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@midl/hardhat-deploy": "^1.0.0",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.0"
  }
}
